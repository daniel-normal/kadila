@model kadila.Models.Contact
@{
    Layout = "_Welcome";
    ViewData["Title"] = "Contacto";
    var successMessage = TempData["SuccessMessage"] as string;
}

<div class="container">
        <h1 class="h1-responsive font-weight-bold text-center my-4">Contáctenos</h1>
        <p class="text-center w-responsive mx-auto mb-5">
            ¿Tienes alguna pregunta? No dudes en ponerte en contacto con nosotros directamente. Nuestro equipo se pondrá en contacto contigo en cuestión de horas para ayudarte.
        </p>
        <div class="row">
            <div class="col-md-9 mb-md-0 mb-5">
                @if (!string.IsNullOrEmpty(successMessage))
                {
                    <div class="alert alert-success text-center" role="alert">
                        @successMessage
                    </div>
                }
                <form asp-controller="Inicio" asp-action="Contact" method="post" id="contact-form" name="contact-form">
                <div class="row">
                    <div class="col-md-6">
                        <div class="md-form mb-0">
                            <input asp-for="Name" id="name" name="name" class="form-control">
                            <label for="name" class="">Nombre y Apellido</label>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="md-form mb-0">
                            <input type="email" asp-for="Email" id="email" name="email" class="form-control">
                            <label for="email" class="">Correo Electrónico</label>
                        </div>
                    </div>
                </div>

                <div class="row mt-lg-4">
                    <div class="col-md-6">
                        <div class="md-form mb-0">
                            <input asp-for="Phone" id="phone" name="phone" class="form-control">
                            <label for="email" class="">Teléfono</label>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="md-form mb-0">
                            <input asp-for="Topic" id="subject" name="subject" class="form-control">
                            <label for="email" class="">Tema</label>
                        </div>
                    </div>
                </div>

                <div class="row mt-lg-4">
                    <div class="col-md-12">
                        <div class="md-form mb-0">
                            <textarea asp-for="Message" id="message" name="message" class="form-control"></textarea>
                            <label for="subject" class="">Mensaje</label>
                        </div>
                    </div>
                </div>
                <div class="text-center text-md-left mt-lg-4">
                    <button type="submit" class="btn btn-primary w-100"><i class="fas fa-paper-plane"></i> Enviar </button>
                </div>
            </form>
            </div>
            <div class="col-md-3 text-center">
                <ul class="list-unstyled mb-0">
                    <li><i class="fas fa-map-marker-alt fa-2x"></i>
                        <p>Bolivia, La Paz</p>
                    </li>
                    <li><i class="fas fa-phone mt-4 fa-2x"></i>
                        <p>591-2-2821591</p>
                        <p>591-2-2824263</p>
                    </li>
                    <li><i class="fas fa-envelope mt-4 fa-2x"></i>
                        <p>kadila@pharmaceuticals.com</p>
                    </li>
                </ul>
            </div>
        </div>
</div>